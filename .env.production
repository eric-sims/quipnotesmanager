# Loaded automatically by `vite build` (production mode) — e.g. the GitHub
# Pages deploy. Points the manager at the real backend instead of localhost.
# The host socket derives wss://api.rotcev.com from this (http->ws swap in
# src/socket.js), so https here gives a secure WebSocket automatically.
VITE_API_URL=https://api.rotcev.com
