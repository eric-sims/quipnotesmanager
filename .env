VITE_API_URL=http://localhost:8081

# Player-client base URL used to build the invite QR/deep-link (?code=1234).
# Local dev: the client dev server (quipnotesclient) runs on :8080.
VITE_CLIENT_URL=http://localhost:8080
